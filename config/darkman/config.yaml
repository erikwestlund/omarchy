# Darkman configuration
# Manual mode only - no automatic sunrise/sunset switching
usegeoclue: false

# Portal integration (exposes preference to apps via XDG portal)
portal: true

# D-Bus API (required for toggle command)
dbusserver: true
