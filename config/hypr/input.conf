# Control your input devices
# See https://wiki.hypr.land/Configuring/Variables/#input
input {
  # Use multiple keyboard layouts and switch between them with Left Alt + Right Alt
  # kb_layout = us,dk,eu
  kb_options = ctrl:nocaps # ,grp:alts_toggle

  # Change speed of keyboard repeat
  repeat_rate = 40
  repeat_delay = 600

  # Start with numlock on by default
  numlock_by_default = true

  # Increase sensitivity for mouse/trackpad (default: 0)
  # sensitivity = 0.35

  touchpad {
    # Use natural (inverse) scrolling
    # natural_scroll = true

    # Use two-finger clicks for right-click instead of lower-right corner
    # clickfinger_behavior = true

    # Control the speed of your scrolling
    scroll_factor = 0.4
  }
}

# Scroll nicely in the terminal
windowrule = scrolltouchpad 1.5, class:(Alacritty|kitty)
windowrule = scrolltouchpad 0.2, class:com.mitchellh.ghostty

# Floating windows
windowrule = float, class:^(floating)$
windowrule = center, class:^(floating)$
windowrule = float, title:^(cheatsheet)$
windowrule = center, title:^(cheatsheet)$
windowrule = size 1200 500, title:^(cheatsheet)$

windowrule = float, title:^(launcher)$
windowrule = center, title:^(launcher)$
windowrule = size 800 600, title:^(launcher)$

# Enable touchpad gestures for changing workspaces
# See https://wiki.hyprland.org/Configuring/Gestures/
# gesture = 3, horizontal, workspace

# =============================================================================
# Per-Device Keyboard Configuration
# =============================================================================
# Swap Alt and Super on laptop keyboard only (external keyboards stay normal)
# Find your device name with: hyprctl devices | grep -i keyboard
#
# Common laptop keyboard names:
#   - at-translated-set-2-keyboard (most laptops)
#   - Framework: at-translated-set-2-keyboard
#
# Uncomment and adjust device name as needed:

# Framework laptop keyboard - swap Alt and Super
device {
    name = at-translated-set-2-keyboard
    kb_options = ctrl:nocaps,altwin:swap_alt_win
}
